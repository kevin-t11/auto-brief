#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Running lint and format checks..."
if bun x lint-staged; then
  echo "✅ All checks passed!"
else
  echo "⚠️ Lint or format issues detected, but commit will proceed."
fi
